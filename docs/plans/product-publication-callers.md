# Product and provenance caller migration

Part of #274 under canonical private-launch tracker #216. The owned caller files are `reconciliation-product-release.spec.ts`, `reconciliation-product-identity.spec.ts`, `reconciliation-provenance-locators.spec.ts` and `reconciliation-curated-lookup.spec.ts`. Current native publication must preserve Product identity, lifecycle, source-specific observations, release/distribution facts, complete inspection, exact approval, replay and diagnostic meaning. Historical query oracles must be moved to retained native evidence where they describe fresh publication; they must not be satisfied by pretending that native publication writes the retired global reconciliation tables.

The reviewed no-change provider `f9249fe8` merges cleanly at `51ce4e053cc1e7ecf24a01ae3ea2da3e089622e5`. Full typechecking passes. PR289 carries the semantic no-change implementation, populated schema30 upgrade correction, independent reviews and separate native/restore validation; its earlier no-change red originated in this caller branch.

The independent Product release and curated lookup files were run together at exact `51ce4e05` on Node22.23.2 with their original test deadlines and a finite360-second outer limit. They completed naturally in58.715 seconds (57.53 Vitest,53.75 tests): eight passed and two failed. Both curated lookup/progress/storage outage/resume cases pass. Six Product cases pass, including native public Product `official_code`, explicit Release events, registered evidence authority and code-preserving refresh, fuzzy-link warnings and role-labelled immutable Printing Images. `/tmp/issue-274-product-release-curated-baseline-51ce4e05.log` retains40 canceled-request and two RPC-disposal warnings; these are not claimed resolved.

The same-authority conflict case reached the intended failed native candidate, whose operation-level code is `printing_reconciliation_blocked`. The fixture now expects that existing native envelope and retains the exact `retained_evidence_invalid` diagnostic with the original same-authority Product detail. This adjustment still needs its focused execution proof.

The other failure is a production currentness gap: the second sealed candidate correctly retains the disappeared Distribution Context with `observed:false` and empty `source_lineages`, but its public export still includes that Context. The original exclusion assertion remains unchanged. Product/context producer and publication ownership are coordinated with the separate membership lane before any production edit. The Product identity and remaining provenance families still require completed migration and validation, including native administrative history support. No ticket completion or integration is claimed by this partial checkpoint.

Three further independent provenance callers now use native operations: reversed observation order retains the consumer revision and relationship export; contradictory locator facts must fail the asynchronously prepared candidate with the original precise diagnostic; and complete native inspection must preserve Card/Printing identities as unchanged or evidence-only while retaining missing-relationship warnings. The inspection case abandons the exact prepared candidate afterward. Native collection state is not presented as a legacy failed/rejected publication run. These three migrations retain their semantic assertions and still await the focused native run; any newly exposed native inspection difference remains a failure to diagnose.

At `33c3fb8e`, the separate reviewed Context filter `5d8838cf` is included. Five focused cases completed naturally in22.573 seconds (21.41 Vitest,17.99 tests): three passed, two failed,14 other cases were filtered. The unchanged disappeared-Context oracle now passes, as do the precise Product conflict diagnostic and reversed-order no-change relationship result. The locator conflict's operation summary intentionally contains only code/detail; the fixture now reads the complete retained candidate warning/shared-warning pages for its original exact locator assertion. The inspection's missing `relationship_not_observed` warning remains a real diagnostic lead: its existing producer consults only the legacy membership table. The original warning assertion is preserved while native history work is coordinated. The run retains12 cancellation and one RPC-disposal warning at `/tmp/issue-274-product-provenance-focus-33c3fb8e.log`.

The two existing Product freshness requirements now exercise the actual administrative `/v1/status` response instead of the retired physical publication table. They require the accepted source run and original captured timestamp, a later accepted same-fact check with the same consumer revision, no change before approval, and no freshness advance when no Product surface was checked. Candidate-only source checks would not satisfy this observable contract. These cases await their native red oracle before any production design or changes.
