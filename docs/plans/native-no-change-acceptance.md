# Native no-change acceptance

The private-launch coordinator confirmed that identical consumer catalogue content must retain its existing consumer revision and export while preserving newly accepted administrative evidence. ADR 0015 replaces multi-game atomic refresh, global predecessor invalidation, synchronous publication and date-crossing invalidation; it does not remove no-change reuse. Issue #216 requires changed/no-change proof and preserves unrelated private consumer/export behavior. The retired `publication_outcome` response field is not a native contract requirement.

## Reproduced failure

The focused test in `reconciliation-provenance-locators.spec.ts` at `3e0247538860a26367dd64ad611e775429addf4a` collected the same retained synthetic source twice, prepared two independently reviewed native manifests and completed both native publications and actual SQL restore checkpoints. Distinct manifests, newer complete successful-check evidence, new snapshots and unchanged first-run snapshots passed. The same-revision assertion failed: a second consumer revision was minted. The run exited naturally in 7.993 seconds (5.602 seconds in the test); it was not interrupted. `/tmp/issue-274-native-nochange-3e024753.log` retains the raw output.

The strengthened oracle at `fba8905b64e2c7597707888d15855483319522e2` compared the integrity-verified `canonical_digest:catalogue` checkpoints. Equality failed before the second publication: `96f57f9dfd3274cc8c6b7aa97cc0a082766deb0d2fc6f0ad4fc5d59d91e5543b` versus `974804d7f7c7b13dfebb1366537a0618d10f516ae9f727e343e4d151b88addcc`. The selected test exited naturally in 6.331 seconds (3.662 seconds in the test); ten other cases were filtered out. `/tmp/issue-274-native-nochange-digest-fba8905b.log` retains the output.

The bounded synthetic comparison at `d66d2a2f` plus a temporary test probe confirmed identical native entity records. Actual verified public export records differed only in the newly minted Card and Printing `last_observed_revision_id` values; image records and other consumer facts matched. The selected test retained its original same-revision failure and exited naturally in 7.738 seconds. `/tmp/issue-274-native-nochange-record-diff-fba8905b.log` retains that output.

A second temporary test probe reopened the integrity-verified semantic reducer prefixes and reconstructed the complete digest input. Both candidates had the identical single Product membership and empty withdrawals. The first retained digest `31bae72a58703adef1810cbf317af829421842c2851003e84c1a89e630c9b08d` exactly matched the reconstructed input without `identity_corrections`; the second `5812fa68fbf8b61869141109934764de1283330d7f38067667320658987607e7` exactly matched the same input with `identity_corrections: []`. No other difference was needed. This probe exited naturally at the unchanged digest-equality assertion in 5.846 seconds (3.17 seconds in the test). `/tmp/issue-274-native-canonical-input-d66d2a2f.log` retains the output. Both temporary probes were removed.

The first narrow correction normalizes an absent and empty identity-correction collection to the historical omitted representation. The streamed draft reader checks at most its first record and closes the iterator; nonempty corrections remain in the semantic digest. At `10bd448fa14c5ab1039de262f3da34a3bdca4a6e`, the repeated-source native test passed digest equality and completed both publications and actual backup checkpoints before failing only its original same-revision assertion. The new locator test retained the same Printing identity, both old/new inspection locators, original snapshots, export identity and export bytes, then failed only semantic digest equality. Both selected tests exited naturally in 12.251 seconds (8.41 seconds in tests); ten other cases were filtered out. `/tmp/issue-274-native-semantic-two-cases-10bd448f.log` preserves the two expected failures.

The separate locator correction removes only top-level `locator_evidence` from semantic Printing facts. It leaves the exact review manifest, retained candidate records and source-defined Game Profile fields intact. The locator-only native regression passed at `2fe2fdf396c07f83ce90ede04c2e18c933aaa833` in 6.077 seconds (3.30 seconds in the test), including retained inspection evidence, original snapshots and immutable public bytes. Two canceled-request warnings appeared during teardown; the command exited successfully. Both semantic corrections received independent Standards and Spec reviews with no findings. This digest proof does not itself prove the no-change publication transaction.

## Bounded acceptance model under review

- Retain the existing streamed catalogue digest at the independent game's seal transaction, where `persistReviewableCandidate` already receives it. Bind the receipt to the candidate and exact manifest; do not hash an assembled game or treat the approval manifest as a semantic digest.
- Keep immutable consumer composition members and existing public artifact identities unchanged on equal consumer content. Record the new exact publication approval and accepted administrative evidence separately.
- Pin the accepted evidence predecessor when a native game preparation is created. Subsequent reconciliation, identity lookup and candidate inspection must use that immutable pin, rather than resolving a mutable latest evidence head during continuation.
- Maintain a current accepted candidate per game, separate from its consumer revision. One game slot and the exact predecessor, approval, writer generation and original deadline remain enforced by the final database transaction.
- Permit multiple accepted candidate bindings to the same consumer revision for lifecycle references. The current `catalogue_candidate_publications.catalogue_revision_id UNIQUE` constraint cannot represent this without a deliberate migration.
- Reserve and actually verify a new backup for the accepted evidence mutation. The next final publication must await the latest accepted mutation's checkpoint. The existing predicate accepting any verified backup for the revision is insufficient after a same-revision acceptance.
- Keep all work bounded: use retained digest receipts, exact candidate identifiers and fixed-size game/composition metadata. Existing streamed preparation and immutable artifact reuse remain in place. No catalogue-sized final transaction, metadata aggregation, global content scan or synthetic checkpoint admission is permitted.

Migration `0031_native_no_change_acceptance.sql` belongs to this lane. The coordinator approved the transaction and evidence-pin model after the native digest oracle passed. The independently reviewed native owner-correction provider `4705f498c0f2a53006d8ba0c8533980ea2ac2602` is incorporated before dependent evidence-pin changes.

## Required proof before integration

Exercise the native owner/API path for equal content with newer evidence, changed content following that accepted refresh, exact approval replay, stale generation/predecessor, expiry, unrelated-game contention, delayed or failed latest backup and actual SQL restore of the accepted evidence. Preserve current-plus-two distinct consumer revision queryability. Compare unchanged public export identities and bytes, and verify administrative evidence separately. Run appropriate focused and full validation and independent Standards/Spec reviews at fixed commits. No private-launch ticket or live gate is fulfilled by this plan alone.

## Acceptance bookkeeping checkpoint

The first transaction checkpoint adds immutable sealed semantic receipts and creation-time predecessor pins, a current accepted candidate per game, and a singleton latest accepted publication. Changed publication already advances these records with its existing atomic transaction. The candidate-to-publication binding permits several accepted candidates at the same immutable revision; its retained-record, recovery and baseline-handoff guards are recreated. Backup snapshot verification includes all four new metadata tables.

The publication checkpoint now requires a verified backup associated with the exact latest accepted operation. A retry inherits that operation and its collection identity from the exact linked reservation, independently of the unchanged consumer revision's original publication. Missing parents, missing operation metadata and mismatched operation metadata fail closed. Four focused real-SQLite repository cases pass, covering older verified backups, pending retry, the exact verified retry, absent acceptance metadata and three invalid retry identities. This is repository-level proof, not an actual backup/restore claim.

All migrations through 0031 apply to an empty real SQLite database and its foreign-key check is empty. Typechecking passes. This checkpoint does not yet implement the same-revision acceptance transaction or its Workflow path; the native no-change publication assertion remains outstanding. Native identity consumers await the separately reviewed owner-correction provider. Existing pre-migration candidates have no newly asserted semantic receipt; no old digest is silently reclassified under the corrected semantic projection.


## Same-revision transaction checkpoint

The Workflow classifies retained equal semantic receipts before public export generation. Its final operation still verifies the candidate's private preparation, exact approval, manifest, inspection receipt, generation, deadline, game slot and immutable evidence predecessor. The no-change branch verifies the existing bounded public manifest bytes and atomically records the new candidate binding, accepted evidence head, completed operation and reserved backup. It does not mutate consumer revisions, composition members, query retention or export objects. Unrelated-game composition contention retries against the latest immutable public root; the final current-revision and latest-backup checks remain authoritative.

The pinned predecessor now feeds native prior-state and locator lookup, candidate inspection, and public lifecycle preparation. Current owner-assignment reviews read the accepted evidence head, then recheck their exact reviewed preparation during decision insertion so unchanged consumer revisions cannot hide a concurrent evidence refresh. Historical composition reads and legacy preparation fallbacks remain explicit.

Manual native backups resolve the latest accepted operation after checking an exact existing reservation or retry parent first. A new `publication_reserved` field keeps one atomic reserved root per publication while allowing separately requested manual roots. The immutable consumer revision retains its original publication lineage. Current administrative backup inspection and production-release recovery gates require the latest accepted operation; older retained revisions require their own original publication's verified backup. Missing native acceptance metadata fails closed.

Ten lightweight SQLite cases pass for exact latest backup admission and administrative inspection, manual backup identity, pending/verified retries, invalid retry metadata, immutable predecessor pins and historical fallbacks. The manual-backup case first failed because it returned the original publication instead of the refresh operation. All migrations through 0031 apply with no foreign-key violations. Typechecking and catalogue dependency checks pass. The native same-revision transaction, manual backup/restore, changed successor, contention, expiry, nonempty correction and retention proofs are still pending. Accepted private-artifact verification coverage is being completed before integration; no completed native publication or launch claim is made at this checkpoint.


## First native acceptance and recovery review

At `dce40725ecb4136926bd6d27a4bdc1c73fa00c2b`, the repeated-source owner journey passed 1/1 with ten unrelated cases filtered out, using Node 22.23.2. Two exact approvals retained the same consumer revision/export while newer checks and snapshots remained available; both backups completed actual SQL export, independent restore and verification. It exited naturally in 8.363 seconds (7.39 seconds Vitest, 5.14 seconds test), below unchanged 30-second test and hook bounds and a finite 90-second outer bound. Four canceled-request warnings appeared in teardown and remain recorded. Raw output: `/tmp/issue-274-native-nochange-dce40725.log`.

Independent Standards and Spec reviews found no actionable issues in bookkeeping `2fe2fdf3..8b9e9981` and core transaction `096c62f9..3a2324f0`. Spec review of the subsequent private-artifact verification found a historical recovery error: recovery validates artifacts through the active/control database, so selecting current accepted heads would replace the target backup's evidence and fail when games were added afterward. That finding is not dismissed by the first native green.

The correction captures at most five exact accepted private roots in the composition snapshot, then recomputes them from restored SQL for snapshot equality. Both live backup verification and historical recovery receive the exact captured list. A schema31 snapshot missing that list fails closed. Legitimate schema30 snapshots retain their earlier metadata/table contract; the new accepted-head tables and partition fingerprint are not queried from databases predating that migration.

Three new bounded native protocol journeys are prepared for runtime verification: failed accepted-root backup and exact actual-restored retry plus a fresh manual root; changed/no-change successors with retained current-plus-two queries, unrelated-game publication contention and historical artifact verification after game membership changes; and nonempty owner corrections followed by semantic no-change reuse. Each new multi-publication journey has a 120-second test bound, with the established 15-second per-operation helper bounds unchanged. Runtime results for the committed cases are recorded below.


## Extended native protocol proof

All three cases in `native-no-change-publication.spec.ts` passed at exact `cb42d860` under Node 22.23.2, naturally in 42.561 seconds (41.40 seconds Vitest; 39.39 seconds in tests):

- Exact final generation, deadline and predecessor failures rolled back sibling writes. Same-revision approval replay retained its result. A missing newly accepted private root caused an actual backup failure; an older verified backup did not reopen publication. The exact linked retry completed real SQL restore verification, then a separately requested manual backup recorded the latest operation and verified successfully. Case runtime: 9.822 seconds.
- A nonempty owner Card merge changed the semantic digest, consumer revision and public correction records. Historical Card bytes remained available. Repeating those corrected facts retained the revision. Case runtime: 13.119 seconds.
- Changed successors preserved three distinct queryable consumer revisions. Adding Fusion World archived the fourth-oldest query revision; a pending One Piece no-change acceptance then reused that newer composition without moving the query window. The original backup's captured private roots verified even though the current accepted head and supported-game membership differed. A schema31 target snapshot missing its accepted roots failed closed. Case runtime: 16.452 seconds.

Raw output is retained at `/tmp/issue-274-native-nochange-protocol-cb42d860.log`. The run emitted 22 canceled-request warnings during Workflow activity/teardown; these were not suppressed or dismissed as an integration pass. No worker/test process remained after completion. This is bounded protocol and actual backup/restore proof, not the complete #276 historical recovery campaign or a hosted capacity result.

Both independent reviews are clear for the final checkpoint: Standards reviewed cumulative `3a2324f0..cb42d860` and incremental `dce40725..cb42d860`; Spec reviewed the same recovery correction and confirmed the previously reported target-selection defect is resolved. Earlier core/bookkeeping reviews remain clear. Ten lightweight SQLite cases, typechecking, import boundaries and cycle checks also pass. Existing native publication regression families, full relevant validation and coordinator integration checks remain outstanding.
