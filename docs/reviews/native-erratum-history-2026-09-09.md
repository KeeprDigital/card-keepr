# Native Erratum private lifecycle

Source caller migration `44f68629ebc3e00af21aecc4baa0bf4f8c2cdd25` exercised native publication. Its full 25-test run at `0988e2bd` finished naturally with 21 passes and four failures in 55.031 seconds. Two failures concern native administration Printing lookups and remain with the source provider. The other two exposed an incorrect fixture migration: original private Erratum and per-provenance first/last-observed assertions had been moved into consumer exports, whose ErratumRecord schema has no lifecycle property.

The first correction, `78ab6850`, wrongly added that public field. At composed `51f04a8ecb130f980841ee2d6ee76e39969f7f6c`, both selected tests failed naturally in 7.929 seconds with `public_export_record_invalid`. The runtime and schema evidence superseded the initial Spec review. This failed implementation is not an accepted consumer contract change.

Correction `0637c5908f605ad1baa0e6291720bd9c4d8d6a34` restores the original consumer schema. The net production change uses existing native lifecycle storage for private Erratum history. Cumulative provenance advances last-observed history only when retained evidence changes; omission and carry preserve earlier history. The exact candidate's Erratum and per-lineage target history are checked through real immutable first/last publication bindings. Exported Errata remain unchanged and explicitly have no lifecycle field. No date activation, new schema, publication guard or deadline is introduced.

Revised fixed-base Standards and Spec reviews of the three-file correction, `2c1fe99b..0637c590`, are clear. The original source caller migration has independent Standards review; its only Spec finding was the corrected private/public history assertion. Types and focused lint/format checks pass. The composed provider merge retains the already-reviewed accepted-evidence identity pin instead of reverting to consumer-composition pins.

At composed `9aa58458c83c275072dff4cf5693af52479b79b7`, both corrected original journeys pass naturally: 2/2 in 19.656 seconds harness time (18.40 Vitest), with 9.709 seconds for disappearance and 6.305 seconds for layering. The outer 180-second allowance includes two unchanged 30-second tests and setup/teardown. Node 22.23.2 ran exclusively on this host. The other 23 tests were explicitly unselected; the full 25 await the independent native administration provider.

A subsequent characterization run at `3069a9b871a6c711c82fe02ab6bc34befe823fc8` retained the corrected private assertions but restored the original production lifecycle implementation. Both tests failed naturally in 17.981 seconds (16.92 Vitest): provenance rows existed, but the required private Erratum lifecycle rows were missing. This separates the real production omission from the discarded public-field assertion. The baseline was recorded after the successful correction; it is not presented as a test-first implementation sequence. No run was interrupted.

[Evidence manifest](evidence/native-erratum-history-20260909/manifest.json) records exact commands, heads, timings and SHA-256 hashes for compressed raw logs, including the discarded attempt and corrected baseline. Final integrated full validation remains required. #274 and #271 remain open.
