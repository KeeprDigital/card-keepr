# Native prior Printing identity — 2026-09-09

Part of #274 and private launch tracker #216. No live operation was performed.

## Fixed provider and behavior

Code: `9a43a400a0354c06e74fa077359bb27228605550`, reviewed against fixed base `e0a74fd910430858eb9868b3409e9a3ad008b601`. The base includes the faithful native backup fixture from `a8dafc76` and the independently reviewed idle-R2 reset fix `c455f841`.

Native publication retains source compatibility evidence outside the legacy identity tables. Subsequent preparations previously queried those empty legacy tables: weak evidence incorrectly sealed and proven official artwork could allocate a duplicate Printing. The fix copies integrity-checked compatibility from the exact predecessor's completed reduction into a private, card-indexed prior identity view. An unobserved Printing carries its prior identity forward. Native matching keeps the existing eight-match/512KB bounds and uses the completed prefix. Public manifests and database schemas do not change.

The five owned game integrity, operation, resource, fencing and dispatch test files use actual native collection/preparation/inspection/publication seeds. The canonical legacy-JSON case retains its historical reserved-export recovery contract.

## Evidence

| Check | Result |
| --- | --- |
| Original full family at `c258de287fda58267bf1611548f62feda0570905` | 54 passed, 1 failed; 55 tests, 5 files; natural exit 1 in 136.230s. The sole failure was the native ambiguous-review case. |
| Isolated retained-data probe | Native published Printing/mapping contain compatibility; legacy compatible/cross-source lookups return empty. Intended failure reproduced in 6.453s. An earlier probe used the wrong public evidence field and failed before the target assertion; that log is retained separately. |
| Four-case red at unchanged production plus `e0a74fd9` | 4 failed at intended assertions in 18.097s: direct weak evidence, carried weak evidence, differing artwork evidence, and positive official-artwork identity reuse. |
| Same four cases with fix | 4 passed in 18.513s. |
| Full owned family at exact clean `9a43a400` | **58 passed, 5 files; natural exit 0 in 145.276s** (Vitest 144.33s, test time 137.65s). |
| Static checks | Full repository typecheck and focused Biome/diff checks pass. |
| Independent fixed-base reviews | Standards and Spec both clear; review does not substitute for integration checks. |

The runtime selections used Node 22.23.2, one worker and unchanged individual test deadlines. Four-case outer bound: 180s. Full family outer bound: repository ingestion shard bound, 2160s. None was interrupted. Raw workerd canceled-request, engine, disposal and injected-storage diagnostics are preserved even where the selected tests pass.

Retained artifacts: `/tmp/card-keepr-launch-20260909/issue-274-native-ambiguity/`, including `red4`, `green4`, `full58` logs and command/commit/timing metadata, probe patches, static logs and SHA-256 `evidence-manifest.json`. Original five-file results remain in adjacent `issue-274-native-fixtures-provider/`.

This is the owned-family proof. Whole integrated-suite and private-launch acceptance remain coordinator gates; #274 and its launch parents are not closed by this result. Native prior/locator ownership transfers to the accepted-evidence predecessor lane only at the reviewed, validated provider above.
