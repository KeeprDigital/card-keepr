# Historical prototypes

These local, synthetic demonstrations are retained executable experiments, not
maintained specifications or production implementations. Each directory's
`cli.mjs` runs its scenarios. Current behavior belongs in
[interface contracts](../contracts/README.md) and
[architecture](../docs/architecture.md); older proposals do not override them.
